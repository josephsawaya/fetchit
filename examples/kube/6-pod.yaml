apiVersion: v1
kind: Pod
metadata:
  name: colors_pod4
spec:
  containers:
  - name: colors-kubeplay
    image: docker.io/mmumshad/simple-webapp-color:latest
    ports:
    - containerPort: 8080
      hostPort: 7030
    envFrom:
    - configMapRef:
        name: env
        optional: false